#!/usr/bin/env bash

source "${BASH_SOURCE%/*}/.helpers.sh"

read -p "THIS WILL DESTROY EVERYTHING. Ctrl-C to abort or enter to continue."

for ENV in development test
do
  docker_compose_in_env down --volumes
done

docker_compose_shared down --volumes
